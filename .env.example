# ============================================
# PostFlow — Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# Required services: Supabase (free), Meta Threads API (free), Vercel (free)
# Optional services: Instagram Graph API (free), Groq AI (free)
# Total cost: $0/month on free tiers
# ============================================

# --------------------------------------------------
# SUPABASE (Required)
# --------------------------------------------------
# Create a free project at https://supabase.com
# Find these in: Project Settings → API
#
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...your-anon-key
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key

# --------------------------------------------------
# THREADS API (Required)
# --------------------------------------------------
# Setup guide: docs/setup-meta-threads.md
#
# Your Threads user ID (numeric). Get it from the API after auth:
#   curl "https://graph.threads.net/v1.0/me?fields=id&access_token=YOUR_TOKEN"
THREADS_USER_ID=

# Long-lived access token (valid ~60 days, auto-refreshed by cron)
# How to get: see docs/setup-meta-threads.md
THREADS_ACCESS_TOKEN=

# Meta App credentials (needed for token refresh)
# Find at: https://developers.facebook.com/apps → Your App → Settings → Basic
THREADS_APP_ID=
THREADS_APP_SECRET=

# Your Threads username (without @). Used to filter out your own replies.
THREADS_USERNAME=

# --------------------------------------------------
# INSTAGRAM API (Optional)
# --------------------------------------------------
# Setup guide: docs/setup-meta-instagram.md
# If not configured, Instagram features are disabled — Threads still works.
#
# Instagram Business Account ID
# Get from: https://developers.facebook.com/tools/explorer/ → /me?fields=id
INSTAGRAM_USER_ID=

# Never-expiring Page Access Token
# How to get: see docs/setup-meta-instagram.md
FACEBOOK_ACCESS_TOKEN=

# --------------------------------------------------
# GROQ AI (Optional — for AI reply suggestions)
# --------------------------------------------------
# Free tier: 6,000 requests/day — more than enough
# Get your key at: https://console.groq.com/keys
GROQ_API_KEY=

# --------------------------------------------------
# AI PERSONA (Optional — customize AI reply personality)
# --------------------------------------------------
# If not set, AI replies use a generic friendly founder persona.
# Customize to match YOUR voice and brand.
#
# Your name (used in AI system prompt)
AI_PERSONA_NAME=

# Short description of who you are (1-2 sentences)
# Example: "Solo founder who built a meditation app after burnout"
AI_PERSONA_DESCRIPTION=

# Your product name
AI_PERSONA_PRODUCT=

# Short product description
# Example: "iOS stress management app with 21 breathing and mindfulness practices"
AI_PERSONA_PRODUCT_DESCRIPTION=

# Tone keywords (comma-separated)
# Example: "witty, self-ironic, honest, casual"
AI_PERSONA_TONE=

# Languages: which language to reply in
# Format: "detect" (auto-detect) or specific like "en" or "uk"
AI_PERSONA_LANGUAGE=detect

# --------------------------------------------------
# SECURITY (Required for production)
# --------------------------------------------------
# Random secret for cron job endpoints (prevents unauthorized triggers)
# Generate: openssl rand -hex 32
CRON_SECRET=

# --------------------------------------------------
# VERCEL (Optional — for auto token refresh)
# --------------------------------------------------
# Only needed if you want automatic Threads token refresh via cron.
# Without this, you'll need to manually update the token every ~60 days.
#
# Get at: https://vercel.com/account/tokens
VERCEL_API_TOKEN=

# Find at: Vercel Dashboard → Your Project → Settings → General → Project ID
VERCEL_PROJECT_ID=

# --------------------------------------------------
# APP URL (auto-set by Vercel, override for local dev)
# --------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000
